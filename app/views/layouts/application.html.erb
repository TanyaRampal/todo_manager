<!DOCTYPE html>
<html>
  <head>
    <title>Todo Manager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="shortcut icon" href="title-image.png" type="image/x-icon" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: "all", 'data-turbolinks-track': "reload" %>
    <%= javascript_pack_tag "application", 'data-turbolinks-track': "reload" %>
  </head>

  <body>
    <% if @current_user %>
      <% if flash[:welcome] %>
        <p class="Flash-welcome">
          <%= flash[:welcome] %>
        </p>
      <% end %>
    <% end %>

    <%= yield %>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script language="JavaScript" type="text/javascript">
      $(function() {
        $("a.Show-remaining-todos").click(function(event){
          event.preventDefault();
          $("div.remaining-todos").toggle();
          $("a.Show-remaining-todos").toggle();
          $("a.Hide-remaining-todos").toggle();
        });

        $("a.Hide-remaining-todos").click(function(event){
          event.preventDefault();
          $("div.remaining-todos").toggle();
          $("a.Show-remaining-todos").toggle();
          $("a.Hide-remaining-todos").toggle();
        });
      });
    </script>

  </body>
</html>
